@model IEnumerable<TestProyect.Models.Adscripcion>

@{
    ViewData["Title"] = "Estatus";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<body class="fondoAdministracion">
    <hr>
    <div class="row">
        <div class="col-md-12">
            <div class="float-left">
                <a asp-controller="Catalogos" asp-action="Index" class="btn btn-primary"><i class="fas fa-arrow-alt-circle-left"> Regresar</i></a>
            </div>
            <div class="float-right">
                <a asp-controller="Catalogos" asp-action="AdscripcionCreate" class="btn btn-info"><i class="fas fa-plus"> Nueva Adscripción</i></a>
            </div>
        </div>
    </div>
    <hr />
    @if (TempData["mensaje"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            <a class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Guardado!</strong> @TempData["mensaje"]
        </div>
    }

    @if (TempData["actualizacion"] != null)
    {
        <div class="alert alert-info alert-dismissible fade show">
            <a class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Actualizado!</strong> @TempData["actualizacion"]
        </div>
    }
    @if (TempData["eliminado"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            <a class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <strong>Eliminado!</strong> @TempData["eliminado"]
        </div>
    }

    <div class="row">
        <div class="col-12">
            <div class="card border">
                <div class="card-header">
                    <h3><strong>Lista de Adscripciones</strong></h3>
                </div>
            </div>
            <div class="card-body card-special">
                <div class="table-responsive">
                    <table class="table table-bordered" id="tablas" width="100%" cellpadding="0">
                        <thead>
                            <tr class="text-center">
                                <th>
                                    @Html.DisplayNameFor(m => m.NombreAdscripcion)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(m => m.Estatus)
                                </th>
                                <th>
                                    Acciones
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                            <tr>
                                <td>
                                    @Html.DisplayFor(m => item.NombreAdscripcion)
                                </td>
                                <td>
                                    @Html.DisplayFor(m => item.Estatus.NombreEstatus)
                                </td>
                                <td class="text-center">
                                    <a class="btn btn-primary" asp-action="AdscripcionEdit" asp-route-id="@item.IdAdscripcion">Editar</a> |
                                    <a class="btn btn-info" asp-action="AdscripcionDetails" asp-route-id="@item.IdAdscripcion">Detalles</a> |
                                    <a class="btn btn-danger" asp-action="AdscripcionDelete" asp-route-id="@item.IdAdscripcion">Eliminar</a>
                                </td>
                            </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</body>

@section Scripts{
    <script>
    $(document).ready(function () {
        $('#tablas').DataTable({
            language: {
                "decimal": "",
                "emptyTable": "No hay Información",
                "info": "Mostrando _START_ a _END_ de _TOTAL_ Entradas",
                "infoEmpty": "Mostrando 0 de 0 Entradas",
                "infoFiltered": "(Filtrando de _MAX_ total entradas)",
                "infoPostFix": "",
                "thousands": ",",
                "lengthMenu": "Mostrar _MENU_ Entradas",
                "loadingMenu": "Cargando...",
                "processing": "Procesando...",
                "search": "Buscar:",
                "zeroRecords": "Sin Resultados Encontrados",
                "paginate": {
                    "first": "Primero",
                    "last": "Ultimo",
                    "next": "Siguiente",
                    "previous": "Anterior"
                }
            }
        });
    });
    </script>
}